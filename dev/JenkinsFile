pipeline {
    agent any
    tools {
       terraform 'terraform'
    }
    environment {
        AWS_ACCESS_KEY_ID     = credentials('jenkins-aws-secret-key-id')
        AWS_SECRET_ACCESS_KEY = credentials('jenkins-aws-secret-access-key')
    }
    stages {
        stage('Git checkout and AWS config') {
           steps{
                git branch: 'dev', credentialsId: 'Github', url: 'https://github.com/accenture-interns2022/terraform-dominos'
                sh 'aws configure set aws_access_key_id $AWS_ACCESS_KEY_ID && aws configure set aws_secret_access_key $AWS_SECRET_ACCESS_KEY && aws configure set region eu-central-1'
            }
        }
        stage('terraform format check'){  {
            sh ''' cd ./dev 
                terraform fmt
            '''}
        }
        stage('terraform Init') {
            steps{
                sh ''' cd ./dev 
                terraform init
            '''}
        }
        stage('terraform Validate') {
            steps{
                sh ''' cd ./dev 
                terraform validate
            '''}
        }
        stage('terraform apply') {
            steps{
                sh ''' cd ./dev 
                terraform apply --auto-approve
            '''}
        }
    }

    
}